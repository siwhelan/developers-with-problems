// import { render, screen } from '@testing-library/svelte';
import { it, describe } from 'vitest';
// import { expect } from 'vitest';
// import NewComment from '../components/NewComment.svelte';

describe('NewComment Component', () => {
	it('renders the NewComment component', () => {
		// 	const mockNewCommentData = {
		// 		loggedInUser: '123'
		// 	};
		// 	render(NewComment, { props: mockNewCommentData });
		// 	const component = screen.getByTestId('"downvote-icon"');
		// 	expect(component).toBeTruthy();
	});
});
